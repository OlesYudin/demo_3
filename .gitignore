# Ignore configuration for Jenkins and Instance
.configuration.sh

# Ingnore private jenkins conf
.jenkins.sh

# Ingnore AWS instant pre-build
.instans.sh

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*
# .tfvars files
*.auto.tfvars

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sentitive data, such as password, private keys, and other secrets.
# *.tfvars

# Ignore override files as they are usually used to override resources locally and so are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore terraform lock
.terraform.lock.hcl